/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.solo.to",
      "links.papareact.com",
      "lh3.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
